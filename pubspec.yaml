name: dart_geo
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  base32: ^1.1.1

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
  geohash: ^0.2.1
